name: pyEBSDIndex
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.8
  - numba
  - scipy 
  - matplotlib 
  - pyopencl
  - jupyter-labs
  #- ocl_icd_wrapper_apple # MacOS
  #- ocl-icd-system # linux (maybe windows?)
  - pip
  - pip:
    - 'ray[default]'
 
